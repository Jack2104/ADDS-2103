#include "DocumentManager.h"
#include "Trie.h"

#include <iostream>

int main() {
    // DocumentManager dm;
    // Trie t;

    // t.insert("Test", 1);
    // t.insert("tesu", 2);
    // t.print();
    // std::cout << t.getDocID("Test") << std::endl;

    return 0;
}